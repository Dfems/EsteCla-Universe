rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Files under uploads/{uid}/...
    match /uploads/{uid}/{allPaths=**} {
      allow read: if true; // public read (you can tighten later)
      allow write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
