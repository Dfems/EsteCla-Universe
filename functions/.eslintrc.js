// Legacy ESLint config (non più usata). L'istanza ufficiale usa eslint.config.js (flat config, ESM).
// Viene intenzionalmente resa no-op per evitare conflitti.
module.exports = {
  ignorePatterns: ['**/*'],
}
